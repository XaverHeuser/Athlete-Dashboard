{% docs staging_activities__doc %}
This view standardizes raw Strava activities for downstream consumption.
- One row per activity
- Timestamps normalized to UTC
- Basic performance fields casted to numeric types
{% enddocs %}